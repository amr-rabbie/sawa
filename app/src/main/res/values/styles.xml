<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/light_green</item>
        <item name="colorPrimaryDark">@color/light_green</item>
        <item name="colorAccent">@color/light_green</item>
        <item name="colorControlNormal">@color/droverbackground1</item>


    </style>
    <style name="AppTheme.Dark.Dialog" parent="Theme.AppCompat.Dialog">
        <item name="colorAccent">@color/white</item>
        <item name="android:textColorPrimary">@color/iron</item>
        <item name="android:background">@color/light_green</item>
    </style>

    <style name="foodRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingbar</item>
        <item name="android:minHeight">40dip</item>
        <item name="android:maxHeight">50dip</item>
    </style>
    <style name="RatingBar" parent="android:style/Widget.Material.RatingBar.Small">
        <item name="android:progressDrawable">@drawable/item_show_rate</item>
        <item name="android:minHeight">20dip</item>
        <item name="android:maxHeight">30dip</item>
    </style>
    <style name="CircularProgress" parent="android:Widget.Holo.ProgressBar">
        <item name="colorAccent">@color/accent_material_light</item>
    </style>
</resources>
